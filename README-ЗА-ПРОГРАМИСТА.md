# Law+ — техническо резюме

**Състояние към 21 август 2026.** Всичко описано тук е компилирано,
стартирано и проверено срещу истински PostgreSQL, истински backend и истински
браузър — не е само написано. Числата в документа са мерени, не оценени.

Пакетът минава през три вълни работа, в този ред:

| | какво | къде са подробностите |
|---|---|---|
| 1 | Извеждане на съдържанието от кода в база + фронтенд като проект + проверка на достъпа на сървъра | този документ, т. 1–4 |
| 2 | Пълен одит по сигурността и поправките от него | `Security-одит-технически.md` |
| 3 | Сесии, устройства, засичане на споделяне, воден знак | `промени/sesii.md`, `промени/ustrojstva-i-signali.md` |

**Дизайнът и функциите на сайта са запазени изцяло.** Нищо не е преработвано
„на ново“: същите екрани, същият вид, същите възможности — включително
помодоро таймера, изпитния жребий, SRS преговора, тетрадката на грешките,
латинския речник, AI асистента и админската конзола.

---

## 1. Какво точно беше счупено

### 1.1. Плащането не пазеше нищо

Учебното съдържание живееше в `pravo-academy-data.js` — **12,5 MB**, които се
сваляха от всеки посетител преди вход и преди плащане. Заключването беше в
браузъра, върху данни, които вече бяха у посетителя. `view-source` ги показва.

Същата дупка беше и в backend-а: `GET /api/content` беше **публичен** маршрут,
който връщаше пълния blob с всички дисциплини. Един адрес, без вход.

> Това е причината за „карта капан“. Не е фронтенд проблем, а продуктов:
> продуктът се раздаваше безплатно на всеки, който погледне.

### 1.2. Съдържанието живееше в кода

Въпроси, конспекти и казуси бяха записани вътре в JavaScript файла. Поправка
на една запетайка изискваше програмист и ново качване на 13 MB. Админският
панел „редактираше“ в localStorage — тоест промяната се виждаше само от
компютъра на админа, а студентите продължаваха да учат старото.

### 1.3. Отговорите пътуваха заедно с въпросите

Верният отговор беше в същия обект, който се подаваше на екрана.

### 1.4. Нямаше система за сглобяване

Един файл от 13 MB, без компилация, без модули, без тестове.

---

## 2. Какво е направено

### 2.1. Съдържанието е извадено в нормализирана база

`content/scripts/extract.mjs` чете стария монолит и произвежда проверени JSON
файлове; внасят се с `backend-patch/scripts/import-content.ts` (идемпотентен,
транзакционен, с `--dry-run`).

Реално внесено и преброено:

| | брой |
|---|---|
| предмети | 11 |
| теми | 509 |
| конспекти | 498 |
| флашкарти | 2 627 |
| тестови въпроси | 4 579 |
| — с избор (`mcq`) | 3 597 |
| — със свободен отговор (`fill`) | 982 |
| казуси | 690 |

Схемата е `backend-patch/migrations/010_content_model.sql`.

**Две неща, които извличането хвана и които биха се загубили тихо:**

* **982 упражнения по латински** се нормализираха като въпроси с избор без
  опции — тоест ставаха неотговорими. Затова има колона `kind`
  (`'mcq' | 'fill'`) с отделни CHECK ограничения. Хвана се чак при истинския
  внос, не при четене на кода.
* **134 флашкарти** нямаха връзка към тема. Възстановени по заглавие;
  проверено — 0 останали.

### 2.2. Фронтендът: същият сайт, вече проект

Един файл от 13 MB → **58 модула + CSS + `index.html`**, сглобени с Vite.

Разделянето е механично, с инструмент, не на ръка: секциите на стария
`app.js` стават модули; `export`/`import` се генерират чрез разбор на кода
(`acorn`), а не с търсене по текст; `main.js` ги внася **в стария ред**,
защото част от тях се изпълняват при зареждане.

| | преди | сега |
|---|---|---|
| първо зареждане | **13 MB** | **469 KB** (95 KB компресиран JS) |
| учебно съдържание в бъндъла | 12,5 MB | **0** |
| админски панел и правни текстове | винаги | теглят се при отваряне |

Подробности — `site/README.md`.

### 2.3. Достъпът се проверява на сървъра

`backend-patch/routes/content.ts`. Едно място решава „има ли право този
потребител“ — `assertTopicAccess`, стъпило на SQL функцията
`user_has_subject(uuid, varchar)`.

**Верните отговори не се селектират заедно с въпросите.** Проверката е
отделна заявка (`POST /content/quiz/:id/check`). Примерното решение на казус
също се тегли отделно, при натискане на бутона.

### 2.4. Админският панел редактира базата

`backend-patch/routes/adminContent.ts` + `site/src/lib/admin-content.js`.
Формите в панела не са пипани — добавен е само преводът между вида на данните
в екрана и вида в API-то.

* Валидация **преди** базата, с грешки по полета (CHECK ограниченията са
  последна преграда, не първа — съобщенията им са на латиница).
* Смяната на опции изисква нов верен отговор — иначе старият индекс сочи вече
  друг текст и въпросът тихо става грешен.
* Обясненията по грешна опция се пазят **по индекс**, не по буква (приемат се
  и букви при внос и се превръщат) — иначе при разбъркване на опциите
  обяснението почва да обяснява друг отговор.
* Всяка промяна оставя следа в `content_revisions` със старата стойност.
* Масовият внос е „всичко или нищо“, с ясен списък какво не е наред.

**Проверено на живо:** редакция от панела → `PATCH 200` → стойността е в
базата → журналът записва кой, кога и какво е било преди.

### 2.5. Поправки в съществуващия backend

| файл | какво |
|---|---|
| `routes/account.ts` | `GET /api/content` вече иска админ. Беше публичен и връщаше цялото платено съдържание. |
| `middleware/errorHandler.ts` | пренася грешките по полета; не пише 4xx в дневника |
| `routes/content.new.ts` | търсенето връщаше суров JSON и не проверяваше достъпа; админът вижда всичко |
| `index.ts` | **изключени стари маршрути**, които четяха платено съдържание без вход (`/api/flashcards`, `/api/cases`, `/api/conspects`, `/api/lectures`, `/api/quiz`, `/api/search`) |
| `index.ts` + `config.ts` | ограничаването на заявките е по потребител, не по IP (виж по-долу) |
| `middleware/optionalAuth.ts` | нов; при срив на базата пада към **по-малко** права |

Подробностите са в `промени/` — по един файл на пипнат модул.

**Отделно: `ОДИТ-НА-BACKEND.md`** казва какво в backend-а е проверено, какво
е поправено и — важното — **какво НЕ е проверявано** (плащанията са в този
списък).

### 2.6. Сигурност — пълен одит и поправки

Отделен документ: **`Security-одит-технически.md`** (архитектура, автентикация,
IDOR, инжекции, тайни, зависимости, TLS/CORS, лични данни, качване на файлове,
подредени находки). Накратко какво е затворено:

| | какво |
|---|---|
| Пароли | bcrypt цена 12, пре-хеширане при вход, постоянно време на отговор при вход (мерено: 356 ms срещу 356 ms за съществуващ и несъществуващ имейл) |
| Обезсилване | `token_version` — изход, смяна и възстановяване на парола свалят старите токени веднага |
| Налучкване | прогресивно заключване (2→60 мин), лимит по потребител, не по IP |
| XSS | 5 дупки във фронтенда, затворени и проверени в истински браузър |
| Права | `authenticate` вече пада „затворено“ при срив на базата; ролята идва от базата, не от токена; 403 вместо 401 при липса на права |
| Одитна следа | `audit_logs` — таблицата липсваше и всеки запис се проваляше тихо |
| Схема | 28 от 85 маршрута връщаха 500, защото кодът четеше несъществуващи таблици и колони (миграция 011) |

### 2.7. Устройства, сесии и споделяне

Един акаунт = едно устройство в даден момент; месечен лимит на различните
устройства с потвърждение по имейл над него; воден знак с имейла върху
материала и при печат; засичане на теглене на едро и на невъзможно движение;
прогресивна реакция (тихо → съобщение → смяна на паролата → човешко решение).

Подробностите и всички превключватели са в `промени/sesii.md` и
`промени/ustrojstva-i-signali.md`.

### 2.8. Съдържание: Облигационно право

Дисциплината е презаредена изцяло от новия материал:

| | брой |
|---|---|
| теми | 78 |
| конспекти | 77 (тема 78 няма подаден текст) |
| тестови въпроса | 780 |
| казуси | 156 |

Всеки въпрос е с 4 опции и обяснение за всяка сгрешена опция плюс методическа
бележка; всеки казус — с 11 полета (подтема, ниво, ключови понятия, учебни
цели, факти, въпроси, насоки, решение, кратък извод, често срещани грешки).
Фронтендът показва всичко това.

---

## 3. Какво намериха проверките

Това не са теоретични подобрения. Всяко от тези е било реален дефект, хванат
при пускане:

1. **Всеки отказ щеше да излиза като „500 сървърна грешка“.** Новите маршрути
   хвърляха собствен клас грешка, който централният `errorHandler` не
   разпознава. Тоест paywall-ът работеше, но се оплакваше грешно.
2. **Обясненията по сгрешена опция никога нямаше да се покажат.** Екранът ги
   търсеше по буква (А/Б/В/Г), а се пазят по индекс.
3. **След вход платеното оставаше празно.** Кешът пазеше отказите отпреди
   входа и не се пресичаше при смяна на правата.
4. **Конспектът зареждаше една тема, а показваше друга.** Екранът чете
   `?chapter=`, а зареждането ползваше `?t=`.
5. **`429 Too Many Requests` при нормална употреба.** 100 заявки / 15 мин по
   **IP адрес** — една университетска мрежа изчерпва квотата за всички.
6. **Тестът по по-задна тема излизаше празен.** Зареждаха се първите 100 от
   750 въпроса; сега избраните теми се теглят поименно.
7. **Търсенето показваше суров JSON** и връщаше откъси от некупени дисциплини.
8. **Без сървър цялата витрина се чупеше** — вместо да покаже рекламните
   страници от кода.
9. **Paywall-ът се заобикаляше с друг адрес.** Освен новите `/api/content/*`
   бяха монтирани и стари маршрути за същото съдържание, без никаква
   проверка. Проверено на живо: `GET /api/flashcards` връщаше платени карти
   на анонимен посетител, а `POST /api/conspects` пишеше без вход.

---

## 4. Как е проверено

Всичко се пуска с една команда и не иска нищо, което го няма в пакета:

```bash
node backend/scripts/proverka/pusni-vsichko.mjs
```

```
Всички маршрути ................ 89 GET · 0 счупени
Съдържанието се показва ........ 19/19
XSS в истински браузър .........  7/7
Страниците се зареждат .........  9/9
Едно устройство наведнъж ....... 34/34
Месечен лимит и засичане ....... 46/46
Устройства — какво вижда човекът 18/18
Сигурност на сървъра ........... 11/11
                                 ─────
                                 144 проверки

Unit тестове (jest) ............ 104/104
tsc --noEmit ................... чисто
```

Единственият маршрут, който не минава, е `/api/subscriptions/products` — иска
истински Stripe ключ. Пропуснат нарочно, не скрит.

**Проверките са писани да падат.** Всяка от тях е пускана и срещу изключена
защита, за да е сигурно, че хваща проблема, а не описва готовия резултат:

| срещу какво | падат |
|---|---|
| изключена проверка на сесията | 6 |
| `DEVICE_LIMIT_ENABLED=false RISK_ENABLED=false BULK_DETECT_ENABLED=false` | 20 |
| стария фронтенд (преди XSS поправките) | 3 от 7 |

Отделно, в `site/tests/` има шест проверки през браузър за неща, които горната
не покрива: 13-те интерактивни функции, админския панел през интерфейса,
поведението при спрян сървър и снимки за сравнение пиксел по пиксел. Искат
акаунт от средата — виж `site/tests/README.md`.

### Дизайнът

Снимки на 10 публични екрана преди и след разделянето — **идентични пиксел
по пиксел** (единственото разминаване е броячът „247 студенти учат в
момента“, който е случаен по замисъл).

### Че платеното наистина не е в бъндъла

Търсене в сглобения JS за `суброгация`, `чл. 79 ЗЗД`,
`Неустойката е предварително`, `Длъжникът изпада в забава` → **нула
попадения**.

---

## 5. Как се пуска

Пакетът е **пълен** — backend, фронтенд и съдържание. Няма какво да се слива
на ръка. Стъпките са в **`КАК-СЕ-ПУСКА.md`**; накратко:

```bash
# база
createdb lawplus
psql lawplus -f backend/src/db/schema.sql
psql lawplus -f backend/migrations/010_content_model.sql
psql lawplus -f backend/src/db/migrations/011_lipsvashti_tablici.sql
psql lawplus -f backend/src/db/migrations/012_sesii.sql
psql lawplus -f backend/src/db/migrations/013_ustrojstva_i_signali.sql

# backend
cd backend && cp .env.example .env      # попълни DB_*, JWT_SECRET, STRIPE_*, SMTP_*
npm install && npm run dev              # порт 3000

# съдържание
npx ts-node scripts/import-content.ts --dry-run
npx ts-node scripts/import-content.ts

# администратор (паролата се подава като аргумент, за да не стои в кода)
node scripts/zadaj-admin.mjs твоя@имейл.bg <парола>

# фронтенд
cd ../site && npm install && npm run build
node serve.mjs dist                     # http://localhost:8080 → API :3000
```

**Задължително преди пускане на живо:** `JWT_SECRET`. По подразбиране е
`change-me-in-production` — с него всеки може да си издаде админски токен.

Има и `Law-plus-demo.html` — целият сайт в един файл, отваря се с двоен клик,
без инсталация и без интернет. За гледане на продукта, не за разработка;
какво в него е истинско и какво е записано предварително е в
`КАК-СЕ-ОТВАРЯ-ДЕМОТО.md`.

---

## 6. Какво остава

Подредено по това кога трябва да се направи, а не по големина. Номерата са, за
да може да се коментира и остойностява точка по точка.

### A. Блокират пускането на живо

| # | какво | защо блокира |
|---|---|---|
| A1 | **SMTP не е настроен** | Потвърждаването на ново устройство и възстановяването на парола минават през пощата. Без нея човек, стигнал до месечния лимит, няма изход. |
| A2 | **Stripe не е тестван на живо** | Ключовете са тестови. `/api/subscriptions/products` иска истински ключ. Плащанията са единственият модул, който изобщо не е проверяван в работещ вид. |
| A3 | **Политиката за поверителност е незапълнен шаблон** | Плейсхолдъри вместо данни на администратора; списъкът с обработващи е грешен и не споменава доставчиците, които реално се ползват. С реални потребителски данни това е правен риск, не козметика. |
| A4 | **Няма срокове за съхранение** | `ai_usage` пази IP адреси, `client_errors` и `stripe_webhooks_log` — пълни събития с лични данни. Без изтриване това расте вечно. |
| A5 | **`JWT_SECRET` и всички тайни** | Трябва да се сменят и да се пазят извън хранилището. |

### Б. Пускане в експлоатация

| # | какво |
|---|---|
| Б1 | Сървър, домейн, сертификат, nginx (проксира `/api`, сервира `site/dist`) |
| Б2 | TLS към базата — `db/index.ts` е без `ssl`; при managed Postgres е задължително |
| Б3 | Архиви — `backup-db.sh` пише некриптирано, на същата машина |
| Б4 | Наблюдение и известяване при срив; днес няма нищо |
| Б5 | Периодичните задачи — `scheduledJobsService.ts` не се вика от никъде и чете колони, които не съществуват (`active` вместо `is_active`, `full_name`, `last_login_at`). Или се поправя и пуска, или се маха. |
| Б6 | CI — проверките съществуват, но се пускат на ръка |

### В. Довършване (не блокира пускането)

| # | какво | бележка |
|---|---|---|
| В1 | **JWT в `localStorage`** | Правилното решение е `httpOnly; Secure; SameSite` бисквитка + краткоживеещ токен в паметта + CSRF защита. Архитектурна промяна, ~2 дни. Дотогава е митигирано: CSP, затворени XSS дупки, адресът на API-то заключен извън разработка. |
| В2 | **2FA за администратор** | Днес админският достъп е само парола. |
| В3 | **Стари зависимости** | `nodemailer` ^6 (8 advisory-та, 2 HIGH), `express-rate-limit` v6 (неподдържан), `stripe` v14 (8 мажора назад). Обновяването е breaking и иска тест на изпращането и на плащанията. |
| В4 | **Rate limit в паметта** | Нулира се при рестарт и не работи при повече от една инстанция. Redis вече е в стека; `rate-limit-redis` е един пакет. Същото важи и за кеша на сесиите. |
| В5 | **Enumeration при регистрация** | `Email already registered` издава кой имейл има акаунт. При входа това вече е затворено. |
| В6 | **Няма проверка срещу изтекли пароли** | zxcvbn или HIBP k-anonymity. |
| В7 | **Имейл адреси в конзолните логове** | `emailService.ts` и др. |
| В8 | **Геолокация** | Засичането на невъзможно движение работи и без нея, но с груба точност. Инсталира ли се `geoip-lite`, кодът сам започва да смята истинско разстояние — без промени. |
| В9 | **Фронтендът носи стария стил** | Глобални функции, шаблонни низове, `innerHTML`, `onclick` през мост към `window`. Разделянето нарочно не пипна съдържанието на функциите — целта беше нулева промяна в поведението. Почистването е по един файл и не бърза. |
| В10 | **Няма типове във фронтенда** | `jsconfig.json` + `// @ts-check` по файлове е евтиното начало. |

### Г. Съдържание (не е програмиране)

| # | какво |
|---|---|
| Г1 | Тема 78 на Облигационно право няма конспект — липсва подаден текст |
| Г2 | Останалите 10 дисциплини са със старото съдържание; преработват се по същия ред като Облигационно право |

---

## 7. За остойностяването

Предишната оценка от 250–300 часа предполагаше започване от нулата. Това вече
не е изходната точка. Налични и проверени са: схемата на базата, извличането
на съдържанието с решените при него проблеми, проверката на достъпа на
сървъра, админското API, фронтендът със сглобяване и модули при запазен
дизайн, одитът по сигурността с приложените поправки, механизмът срещу
споделяне на акаунти, и проверките към всичко изброено.

Онова, което остава за остойностяване, е списъкът в точка 6 — по групи:

* **A** — пет неща, без които не се пуска с реални потребители
* **Б** — пускането в експлоатация
* **В** — довършване, което може да върви и след пускането
* **Поддръжка** — отделно от горните: реакция при проблем, обновявания,
  дребни поправки

Точка 6 е нарочно номерирана, за да може всяка да се обсъди поотделно — и за
да е ясно кое е задължително преди пускане и кое може да изчака.

Ако при прегледа изникне нещо, което не е в този списък, то е пропуск в
документа, не скрито наум. Всичко, което знам, че е отворено, е горе.
